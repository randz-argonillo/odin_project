<%= link_to gravatar_for(user, size: "50"), user %>
<h1><%= user.name %></h1>
<span class="view_profile"><%= link_to "view my profile", user %></span>
<span class="microposts"><%= pluralize(user.microposts.count, "micropost") %></span>